apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    isDefault: true
    editable: true
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: private
      defaultBucket: my-room-monitor
    secureJsonData:
      token: my-secret
